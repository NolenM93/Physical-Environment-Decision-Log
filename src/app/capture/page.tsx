import { CaptureWizard } from '@/components/capture/CaptureWizard';

export const metadata = {
  title: 'Capture a room — Stanza',
};

export default function CapturePage() {
  return <CaptureWizard />;
}
